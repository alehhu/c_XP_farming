
























while(1) {
    printf("My Shell> ");

    /**
    * 
    * char *token, *string = getline(char **lineptr, size_t *n, FILE *stream);

    * the first parameter of the getline function is the memory address where the captured line is gonna be saved to. By passing NULL, it's going to allocate with malloc the exact amount of memory needed.
    *
    */
    char *token, *string = getline(char **lineptr, size_t *n, FILE *stream);





}
