#include <stdio.h>
#include <linked_list.h>


struct Element {


    struct Element next;
};


/**
 * Requires the struct passed as value.
 * This makes the addition slower, but the access to the element eventually faster in the future, since the elements are gonna all mapped together in memory.
 *
 *
 */
int addElement(struct Element element){
    
}



int main() {

    


    return 0;
}
